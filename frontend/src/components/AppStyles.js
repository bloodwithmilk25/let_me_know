export default {
  // Prevent page scrolling when modal is open.
  "@global": {
    a: {
      "&:link": {
        textDecoration: "inherit",
        color: "inherit",
        cursor: "pointer"
      },
      "&:visited": {
        textDecoration: "inherit",
        color: "inherit",
        cursor: "auto"
      }
    }
  }
};
